apiVersion: v1
kind: Service

metadata:
  name: node-exporter
  namespace: monitoramento
  labels:
    app: node-exporter

spec:
  type: ClusterIP

  selector:
    app: node-exporter

  ports:
  - name: metrics
    port: 9100
    targetPort: metrics
    protocol: TCP